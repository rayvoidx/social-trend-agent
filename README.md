# 소비자 트렌드 자동 분석 에이전트

> 뉴스·SNS·동영상 채널의 트렌드를 **자동 수집·분석·요약**하여 마케팅·상품기획 의사결정을 지원하는 **멀티 에이전트 시스템**

![Python](https://img.shields.io/badge/python-3.11+-blue.svg)
![LangGraph](https://img.shields.io/badge/LangGraph-Latest-orange.svg)
![Docker](https://img.shields.io/badge/Docker-Ready-blue.svg)

---

## 🎯 주요 기능

- **자동 데이터 수집**: NewsAPI, Naver News, YouTube (API 키 없으면 샘플 데이터로 fallback)
- **감성 분석**: 긍정/부정/중립 자동 분류
- **키워드 추출**: 핵심 키워드 및 트렌드 파악
- **바이럴 탐지**: 급상승 콘텐츠 자동 감지
- **리포트 생성**: 마크다운 형식의 분석 리포트
- **자동화 지원**: n8n, Slack 웹훅 연동

---

## 🗂 에이전트 구성

| 에이전트 | 기능 | 산출물 |
|---------|------|--------|
| **news_trend_agent** | 뉴스 트렌드 분석 | 감성 분석 + 키워드 + 인사이트 리포트 |
| **viral_video_agent** | 바이럴 영상 탐지 | 급상승 영상 랭킹 + 성공 요인 분석 |

---

## 🏗 시스템 아키텍처

```
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  CLI Runner  │  │  n8n Webhook │  │  API Server  │
│  (scripts/)  │  │  (Automation)│  │  (Optional)  │
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       │                 │                 │
       └─────────────────┴─────────────────┘
                         │
       ┌─────────────────▼─────────────────┐
       │   LangGraph Orchestration         │
       │  collect → normalize → analyze    │
       │  → summarize → report → notify    │
       └─────────────────┬─────────────────┘
                         │
       ┌─────────────────▼─────────────────┐
       │   Agents (news / viral video)     │
       └─────────────────┬─────────────────┘
                         │
       ┌─────────────────▼─────────────────┐
       │   Shared Utilities                │
       │   retry / cache / logging /       │
       │   error_handling                  │
       └─────────────────┬─────────────────┘
                         │
       ┌─────────────────▼─────────────────┐
       │   External Services               │
       │   NewsAPI / Naver / YouTube       │
       │   Azure OpenAI / Slack / n8n      │
       └───────────────────────────────────┘
```

---

## ⚙️ 설치 및 실행

### 1. 환경 설정

```bash
# 1. 레포지토리 클론
git clone https://github.com/rayvoidx/Automatic-Consumer-Trend-Analysis-Agent.git
cd Automatic-Consumer-Trend-Analysis-Agent

# 2. .env 파일 설정
# .env 파일을 열어서 Azure OpenAI API 키 입력
# LLM_PROVIDER=azure_openai
# OPENAI_API_KEY=your-key-here
```

### 2. 실행 방법

**옵션 A: Docker (권장)**

```bash
docker compose up -d --build
```

**옵션 B: 로컬 Python**

```bash
# 의존성 설치
pip install -e .

# 에이전트 실행
python scripts/run_agent.py --agent news_trend_agent --query "AI trends" --window 7d
```

---

## 🔬 사용 예시

### 뉴스 트렌드 분석

```bash
python scripts/run_agent.py \
  --agent news_trend_agent \
  --query "전기차" \
  --window 7d
```

**출력**: `artifacts/news_trend_agent/{run_id}.md` 리포트 생성

### 바이럴 영상 탐지

```bash
python scripts/run_agent.py \
  --agent viral_video_agent \
  --market KR \
  --platform youtube
```

**출력**: `artifacts/viral_video_agent/{run_id}.md` 리포트 생성

---

## 📁 프로젝트 구조

```
.
├── agents/                  # 에이전트 구현
│   ├── news_trend_agent/    # 뉴스 트렌드 분석
│   ├── viral_video_agent/   # 바이럴 영상 탐지
│   └── shared/              # 공유 유틸리티 (retry, cache, logging)
├── scripts/                 # 실행 스크립트
│   └── run_agent.py         # CLI 러너
├── automation/              # 자동화 설정
│   └── n8n/                 # n8n 워크플로우
├── artifacts/               # 분석 결과 저장
├── docker-compose.yaml      # Docker 설정
├── Dockerfile               # 컨테이너 이미지
├── pyproject.toml           # Python 의존성
└── .env                     # 환경 변수
```

---

## 🔑 환경 변수

### 필수 설정

```bash
# LLM 제공자 (azure_openai, openai, anthropic, google, ollama 중 선택)
LLM_PROVIDER=azure_openai

# Azure OpenAI 설정
OPENAI_API_TYPE=azure
OPENAI_API_BASE=https://your-resource.openai.azure.com/
OPENAI_API_KEY=your-api-key
OPENAI_DEPLOYMENT_NAME=gpt-4
```

### 선택 설정

```bash
# 데이터 소스 (없으면 샘플 데이터 사용)
NEWS_API_KEY=
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=
YOUTUBE_API_KEY=

# 알림
SLACK_WEBHOOK_URL=
N8N_WEBHOOK_URL=
```

---

## 🤖 n8n 자동화

`automation/n8n/` 디렉토리의 워크플로우를 n8n에 가져오기:

1. **news_daily_report.json**: 매일 아침 자동 뉴스 분석
2. **viral_spike_alert.json**: 급상승 콘텐츠 실시간 알림

---

## 📊 기술 스택

- **LangGraph**: 에이전트 워크플로우 오케스트레이션
- **LangChain**: LLM 통합
- **Pydantic**: 데이터 검증
- **FastAPI**: REST API (선택 사항)
- **Docker**: 컨테이너화

---

## 📝 라이선스

MIT License

---

## 🔗 참고

- 각 에이전트의 `README.md`: 상세 사용법 및 빠른 검증 가이드
